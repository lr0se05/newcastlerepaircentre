services:
  newcastlerepaircentre-web:
    image: nginx:alpine
    container_name: newcastlerepaircentre-web
    ports:
      - "8090:80"
    volumes:
      - ./website:/usr/share/nginx/html:ro
    restart: unless-stopped